import { Action } from '../core/Action';

export const EXAMPLE_ACTION = {
    entity: 'EXAMPLE_ACTION',
    action: Action.HANDLE,
};

export const EXECUTE_QUERY = {
    entity: 'EXECUTE_QUERY',
    action: Action.HANDLE,
};
export const EXECUTE_QUERY_HTML = {
    entity: 'EXECUTE_QUERY_HTML',
    action: Action.HANDLE,
};

export const SAVE_QUERY = {
    entity: 'SAVE_QUERY',
    action: Action.HANDLE,
};

export const GET_ALL_QUERIES = {
    entity: 'GET_ALL_QUERIES',
    action: Action.HANDLE,
};

export const GET_ALL_QUERIES_PUBLIC = {
    entity: 'GET_ALL_QUERIES_PUBLIC',
    action: Action.HANDLE,
};

export const GET_SINGLE_PUBLIC_QUERY = {
    entity: 'GET_SINGLE_PUBLIC_QUERY',
    action: Action.HANDLE,
};

export const GET_SAVED_QUERY_RESULT = {
    entity: 'GET_SAVED_QUERY_RESULT',
    action: Action.HANDLE,
};
